# pip install -r requirements.txt

fastapi==0.110.0
uvicorn[standard]==0.27.1
sqlalchemy==2.0.28
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                # pinned — newer bcrypt breaks passlib's version check
python-multipart==0.0.9
Pillow>=10.4.0
aiofiles==23.2.1

# task queue
celery==5.3.6
redis==5.0.1

# vision model (MobileNetV3-Small, pretrained on ImageNet, used as a
# feature extractor — see app/services/vision_model.py for the full story)
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.24.0
